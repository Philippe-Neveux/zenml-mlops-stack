[project]
name = "zenml-mlops-stack"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.3.2",
    "pip>=25.2",
    "pyarrow>=21.0.0",
    "scikit-learn>=1.7.1",
    "zenml[server]==0.84.3",
]
